Very, very basic start of the user interface, with a tiny bit of logic to test out some interactions.


index.html is the page that will be seen by clients.  Make sure the two files are in the same directory.